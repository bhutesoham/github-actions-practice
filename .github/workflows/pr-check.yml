name: pr-check

on:
  pull_request:
    branches: [ "main" ]

jobs:

  runs-on: ubuntu-latest
    
    - name: run the PR check for merging branch
      run: echo "PR check running for branch: ${{ github.ref_name }}"
