name: Smart Triggers

on:
  push:
    branches:
      - main
      - "release/*"
    paths:
      - "src/**"
      - "app/**"

jobs:
  run-on-code-change:
    runs-on: ubuntu-latest

    steps:
      - name: Run when src/ or app/ changes
        run: echo "Code changes detected in src/ or app/"
